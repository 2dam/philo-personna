services:
  - type: web
    name: mindset1000
    runtime: node
    plan: free
    buildCommand: echo "No build required"
    startCommand: node server.mjs
    healthCheckPath: /
    autoDeployTrigger: commit
